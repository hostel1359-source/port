'use client';

// Skills are handled by the 3D SkillCloud component
export default function SkillsSection() {
  return (
    <section id="skills" className="min-h-[200vh] relative w-full pointer-events-none" />
  );
}
