'use client';

// GitHub activity is handled by the 3D DeskSetup component
export default function GitHubSection() {
  return (
    <section id="github" className="min-h-screen relative w-full pointer-events-none" />
  );
}
