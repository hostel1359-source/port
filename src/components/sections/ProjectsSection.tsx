'use client';

// Projects are now handled by 3D ProjectDoor components + auto side panels
export default function ProjectsSection() {
  return (
    <section id="projects" className="min-h-[500vh] relative w-full pointer-events-none" />
  );
}
